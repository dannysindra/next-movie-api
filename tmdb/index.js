const { TmdbAPI } = require('./datasource');
const { tmdbResolver } = require('./resolver');
const { tmdbTypeDef } = require('./typedef');

module.exports = {
    TmdbAPI,
    tmdbResolver,
    tmdbTypeDef
};
